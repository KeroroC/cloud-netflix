spring:
  application:
    name: eureka-consumer
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server-git
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/